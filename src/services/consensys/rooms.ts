// import { makeRequest } from "@/services/helpers";
// // import { RoomCreateResponse } from "../types";
// import { AxiosResponse } from "axios";

// // export async function createRoom() {
// //     const res: AxiosResponse<RoomCreateResponse> = await makeRequest({method: 'POST', endpoint: '/api/rooms'});
// //     const currentRoomId = res.data.data.room_id;
// //     alert(`Room created! Room ID: ${currentRoomId}`);
// //     return res;
// // }

// // export async function getRoomStatus(roomId: string) {
// //     const response = await makeRequest({method: 'GET', endpoint: '/api/enhanced/getRoomStatus', data: { roomId }});
// //     return response;
// // }

// // export async function updateRoomConfig(roomId: string, config: unknown) {
// //     // const { roomId, years, genres, votingDuration } = req.body;
// //     const response = await makeRequest('POST', '/api/enhanced/updateRoomConfig', {
// //         roomId,
// //         years,
// //         genres,
// //         votingDuration
// //     });
// //     const response = await makeRequest({method: 'POST', endpoint: '/api/enhanced/updateRoomConfig', data: { roomId, config }});
// //     return response;
// // }
